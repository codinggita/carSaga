$kind: http-request
name: Get Chat By ID
method: GET
url: '{{baseUrl}}/api/chat/{{chatId}}'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
pathVariables:
  - key: chatId
    value: '{{chatId}}'
