$kind: http-request
name: Compare Cars
method: GET
url: '{{baseUrl}}/api/analytics/compare'
order: 1000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
queryParams:
  - key: car1
    value: '{{carId1}}'
  - key: car2
    value: '{{carId2}}'
