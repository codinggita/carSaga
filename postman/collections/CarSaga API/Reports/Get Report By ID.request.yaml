$kind: http-request
name: Get Report By ID
method: GET
url: '{{baseUrl}}/api/reports/{{reportId}}'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
pathVariables:
  - key: reportId
    value: '{{reportId}}'
