$kind: http-request
name: Get Generations
method: GET
url: '{{baseUrl}}/api/car-database/generations'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
queryParams:
  - key: model_id
    value: '{{modelId}}'
