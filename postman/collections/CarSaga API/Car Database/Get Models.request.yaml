$kind: http-request
name: Get Models
method: GET
url: '{{baseUrl}}/api/car-database/models'
order: 2000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
queryParams:
  - key: brand_id
    value: '{{brandId}}'
