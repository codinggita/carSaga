$kind: http-request
name: Get Variations
method: GET
url: '{{baseUrl}}/api/car-database/variations/{{generationId}}'
order: 4000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
pathVariables:
  - key: generationId
    value: '{{generationId}}'
