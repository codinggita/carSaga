$kind: http-request
name: Delete Car
method: DELETE
url: '{{baseUrl}}/api/cars/{{carId}}'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
pathVariables:
  - key: carId
    value: '{{carId}}'
