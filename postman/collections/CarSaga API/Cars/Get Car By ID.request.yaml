$kind: http-request
name: Get Car By ID
method: GET
url: '{{baseUrl}}/api/cars/{{carId}}'
order: 3000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
pathVariables:
  - key: carId
    value: '{{carId}}'
