$kind: http-request
name: Google Auth
method: POST
url: '{{baseUrl}}/api/auth/google'
order: 3000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "credential": "{{googleIdToken}}"
    }
